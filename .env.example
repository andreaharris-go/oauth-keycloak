# IMPORTANT: This is a DEMO configuration only!
# For production use:
# 1. Generate a new client secret: openssl rand -hex 32
# 2. Update KEYCLOAK_CLIENT_SECRET in docker-compose.yml
# 3. Update the secret in keycloak/realm-export.json
# 4. Never commit real secrets to version control
# 5. Use Docker secrets or environment files excluded from git

# Backend Environment Variables
NODE_ENV=development
PORT=3001
KEYCLOAK_URL=http://keycloak:8080
KEYCLOAK_REALM=oauth-demo
KEYCLOAK_CLIENT_ID=nestjs-backend

# SECURITY WARNING: Replace this demo secret before production deployment
KEYCLOAK_CLIENT_SECRET=ffdf702e77fbf9ec0255bdc727964f2dcd5648d65435249414f3a84dc8091f06

# Frontend Environment Variables
NEXT_PUBLIC_API_URL=http://localhost:3001
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8080
NEXT_PUBLIC_KEYCLOAK_REALM=oauth-demo
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=nextjs-frontend
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production

# Database (PostgreSQL for Keycloak)
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=keycloak

# Keycloak Admin
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
